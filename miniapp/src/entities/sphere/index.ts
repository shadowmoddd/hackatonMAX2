// Re-export everything from the legacy data layer
export type { StepStatus, CareerPath, StepMaterial, Step, Sphere } from '../../data/spheres'
export { getSphere, getStep, STEP_QUIZ_QUESTIONS } from '../../data/spheres'
